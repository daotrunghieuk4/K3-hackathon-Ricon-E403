# Backend và tool eval dùng chung Gemini API key.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-flash-latest

# Tùy chọn. Bỏ trống để backend chạy chế độ in-memory fallback.
DATABASE_URL=

# Tùy chọn. Mặc định 5000.
PORT=5000
